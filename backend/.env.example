# Copy to backend/.env (git-ignored) and fill in your local MySQL details.
# Tests use the same server and credentials, with a separate `lyfego_test` database.
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=lyfego
